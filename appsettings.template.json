{
  "_comment": "Copy this to appsettings.json. For production, set GROQ_API_KEY environment variable instead of ApiKey.",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "TextHumanizer": "Debug"
    }
  },
  "AllowedHosts": "*",
  "Cors": {
    "_comment": "Leave empty for development (allows all). Set specific origins for production.",
    "AllowedOrigins": []
  },
  "LlmProvider": {
    "Provider": "Groq",
    "Model": "llama-3.3-70b-versatile",
    "LmStudio": {
      "BaseUrl": "http://localhost:1234/"
    },
    "Groq": {
      "BaseUrl": "https://api.groq.com/openai/",
      "ApiKey": "YOUR_GROQ_API_KEY_HERE (or use GROQ_API_KEY env var)"
    }
  }
}
